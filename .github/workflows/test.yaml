on:
  push:
    branches:
      - main
    tags-ignore:
      - '*'
name: test
jobs:
  uninstall:
    runs-on: master
    steps:
    - name: build
      run: make build
    - name: Checkout code
      uses: actions/checkout@v4
    - name: uninstall
      run: make uninstall
    - name: install
      run: make install